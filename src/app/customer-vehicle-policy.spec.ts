import { CustomerVehiclePolicy } from './customer-vehicle-policy';

describe('CustomerVehiclePolicy', () => {
  it('should create an instance', () => {
    expect(new CustomerVehiclePolicy()).toBeTruthy();
  });
});
