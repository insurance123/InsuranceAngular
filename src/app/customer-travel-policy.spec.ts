import { CustomerTravelPolicy } from './customer-travel-policy';

describe('CustomerTravelPolicy', () => {
  it('should create an instance', () => {
    expect(new CustomerTravelPolicy()).toBeTruthy();
  });
});
